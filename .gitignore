# Python bytecode / caches — the tools/ scripts are the source, not these.
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.claim-review-ack
# The override log records WHY a claim gate was bypassed, in the reviewer's own words.
# It is a local audit trail, not a published artifact, and it is one `git add -A` away
# from being one. Ignored for the same reason the receipts file is.
.claim-review-audit.log
.local-holds/
